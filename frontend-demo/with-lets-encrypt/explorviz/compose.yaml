services:
  frontend:
    container_name: frontend
    image: explorviz/frontend:latest
    environment:
      - VIRTUAL_HOST=${LETSENCRYPT_HOSTNAME}
      - LETSENCRYPT_HOST=${LETSENCRYPT_HOSTNAME}
      - FRONTEND_HOST_URL=http://${FRONTEND_HOSTNAME}:${FRONTEND_PORT} # Determines base URL for backend services that frontend calls
      - FRONTEND_HOSTNAME=${FRONTEND_HOSTNAME}
      - CODE_AGENT_URL=http://${DEMO_HOSTNAME}:${PERSISTENCE_PORT}
      - PERSISTENCE_SERV_URL=http://${DEMO_HOSTNAME}:${PERSISTENCE_PORT}
      - COPILOT_SERV_URL=http://${DEMO_HOSTNAME}:${PERSISTENCE_PORT}
      - USER_SERV_URL=http://${DEMO_HOSTNAME}:${USER_PORT}
      - VSCODE_SERV_URL=http://${DEMO_HOSTNAME}:${PERSISTENCE_PORT}
      - KEYCLOAK_URL=http://${DEMO_HOSTNAME}:${PERSISTENCE_PORT}

  demo-supplier:
    container_name: explorviz-demo-supplier
    image: explorviz/demo-supplier:latest
    expose:
      - 8084
      - 8085

networks:
  default:
    name: nginx-proxy
