version: "3.5"
services:

  ocelot-agent:
    image: inspectit/inspectit-ocelot-agent:1.8.1
    container_name: ocelot-agent
    mem_limit: 128M
    networks:
      - explorviz
    volumes:
      - agent-vol:/agent

  spring-petclinic:
    image: explorviz/spring-petclinic
    container_name: spring-petclinic
    depends_on: 
      - ocelot-agent
    ports: 
      - 18080:18080
    networks:
      - explorviz
    volumes:
      - agent-vol:/agent
      - ./inspectit-docker.yml:/agent/inspectit.yml
    entrypoint: ["sh", "-c", "
    java
    -Dinspectit.config.file-based.path=\"/agent/\"
    -javaagent:/agent/inspectit-ocelot-agent.jar
    -jar /app.jar"]

volumes:
  agent-vol:

networks:
  explorviz:
    name: explorviz